var buttonColors = ["red", "blue", "green", "yellow"];
//var randomChosenColor = buttonColors[Math.random()*3];

var gamePattern = [];
gamePattern.push(buttonColors[Math.round(Math.random()*3)]);

function nextSequence()
{
	var randomNum = Math.random()*3;
}